class Object:
    
    attributes = list()

    def __init__(**kwargs) -> None:
        for kwarg in kwargs:
            self.

    def __iter__(self) -> dict:
        return iter(dict(
            ((key for key in attributes))
        ).items())
